import { TejasabduldirectiveDirective } from './tejasabduldirective.directive';

describe('TejasabduldirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TejasabduldirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
