import { SMDirectiveDirective } from './sm-directive.directive';

describe('SMDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SMDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
