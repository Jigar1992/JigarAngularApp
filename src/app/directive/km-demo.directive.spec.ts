import { KMDemoDirective } from './km-demo.directive';

describe('KMDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new KMDemoDirective();
    expect(directive).toBeTruthy();
  });
});
