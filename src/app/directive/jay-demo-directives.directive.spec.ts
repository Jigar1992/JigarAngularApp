import { JayDemoDirectivesDirective } from './jay-demo-directives.directive';

describe('JayDemoDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new JayDemoDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
